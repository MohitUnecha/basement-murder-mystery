{
  "name": "basement-murder-mystery",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "start:server": "node server/index.js",
    "start:client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
